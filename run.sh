#! /bin/bash

#
# Since our application need shader, and the shader
# are in application directory, so we must ensure that
# we are in the correct directory before run the engine
#

cd linux_build
./TEngine
